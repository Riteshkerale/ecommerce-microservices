spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://db:3306/notification_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: kafka-1:19092,kafka-2:19092,kafka-3:19092

  mail:
    host: smtp.gmail.com
    port: 587
    username: riteshkerale@gmail.com
    password: qpxf ishz cynf khva

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

server:
  port: 8085

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka/